# Variáveis para MCPs avançados (opcional)
# Copie para variáveis de ambiente do Windows ou use em Settings > System > Environment Variables

# MCP Atlassian via Docker (só se usar mcp-atlassian)
ATLASSIAN_EMAIL=seu.email@ab-inbev.com
ATLASSIAN_API_TOKEN=

# MCP Databricks (só se usar databricks)
DATABRICKS_HOST=https://adb-XXXXXXXX.XX.azuredatabricks.net
